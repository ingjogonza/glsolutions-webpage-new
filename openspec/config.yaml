# OpenSpec Configuration — GL Solutions Website Adaptation
# Generated by sdd-init on 2026-06-23

context: |
  Adapting the RicoFast SaaS template (Astro 6.4.2 + Tailwind CSS v4) into a
  corporate website for GL Solutions, an Argentinian accounting and business
  advisory firm. This is a static-first, frontend-only project with no backend.
  The template provides marketing pages, MDX blog/changelog, demo pricing and
  auth screens, dark mode, and a documented design system. The adaptation
  replaces demo content with GL Solutions branding, services, and bilingual
  (ES/EN) support while preserving the foundation.

strict_tdd: false
  # No test runner detected in the template. Strict TDD is disabled until
  # test infrastructure is added. Verification relies on build checks
  # (astro check && astro build) and manual review.

testing:
  test_runner: none
  test_layers:
    unit: { available: false, tool: null }
    integration: { available: false, tool: null }
    e2e: { available: false, tool: null }
  coverage: false
  quality:
    linter:       { tool: biome,     command: "pnpm check" }
    type_checker: { tool: astro/ts,  command: "pnpm build (astro check)" }
    formatter:    { tool: biome,     command: "pnpm check" }

preflight:
  execution_mode: interactive
  artifact_store: both
  chained_pr: ask-always
  review_budget: 400

phases:
  proposal: { required: true, path: proposals/ }
  spec:     { required: true, path: specs/ }
  design:   { required: true, path: designs/ }
  tasks:    { required: true, path: tasks/ }
  apply:    { required: true, path: changes/ }
  verify:   { required: true }
  archive:  { required: true, path: archive/ }
